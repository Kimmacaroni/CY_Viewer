# CY뷰어 작업 지침

이 파일은 저장소 전체에 적용한다. 하위 디렉터리에 더 구체적인 `AGENTS.md`가 있으면 해당 범위에서는 두 지침을 함께 따르며, 충돌 시 하위 지침을 우선한다.

## 언어

- 사용자와의 대화, 화면 문구, 문서와 주석은 한국어로 작성한다.

## 작업 격리와 Git 원칙

- 모든 작업은 최신 `main`에서 만든 전용 브랜치와 별도 worktree에서 수행한다. 기존 작업 디렉터리, 다른 작업자의 브랜치 또는 다른 worktree를 직접 수정하지 않는다.
- 작업 시작 전 현재 경로, 브랜치, 기준 커밋, `git status`와 대상 범위를 확인한다. 변경이 이미 있으면 덮어쓰거나 임의로 정리하지 말고 소유자를 확인한다.
- 작업마다 `docs/WORK_LOG.md`에 시작 기록을 먼저 남긴다. 목표, 담당 주체, 브랜치/worktree, 기준 SHA, 대상 파일, 검증 계획을 적는다.
- 한 작업에는 한 목적만 담고, 검토 가능한 크기의 커밋으로 남긴다. 요청받지 않은 원격 변경, 강제 푸시, 기록 재작성, 태그 생성 또는 릴리스를 하지 않는다.
- 완료 시 같은 작업 기록에 결과, 변경 파일, 실행한 빌드·테스트와 결과, 미검증 항목, 후속 작업, 최종 커밋 SHA를 보완한다.

## 인수인계와 기록

- 작업을 시작하기 전에 `docs/HERMES_AGENT_HANDOFF_KO.md`와 `docs/WORK_LOG.md`를 읽는다.
- 구조, 버전, 기능, 빌드·테스트 절차, 배포 경로, 기준 구현체 또는 알려진 위험이 바뀌면 같은 커밋에서 `docs/HERMES_AGENT_HANDOFF_KO.md`를 최신화한다.
- 실행하지 않은 검증을 성공으로 기록하지 않는다. 실행 환경이 없어 검증하지 못했다면 이유, 영향 범위, 필요한 플랫폼과 정확한 재검증 명령을 남긴다.
- 의사결정이 필요한 사항은 추측으로 확정하지 말고 선택지, 영향, 결정권자를 기록한다.

## 보안과 문서 데이터

- 비밀번호, 토큰, API 키, 인증서·개인키, 서명 프로파일, 환경 파일 등 비밀정보를 커밋하지 않는다.
- 고객 PDF, 실제 업무 문서, 개인정보·민감정보가 든 파일, 해당 파일에서 추출한 OCR 결과·스크린샷·임시 산출물을 커밋하지 않는다.
- 테스트에는 저장소에 사용 허가가 명확한 비민감 합성 fixture만 사용한다. 로컬 고객 문서가 필요했던 재현 절차에는 파일명이나 내용을 기록하지 않고 익명화한 특성만 남긴다.
- 커밋 전에 추적 파일과 diff를 확인해 비밀정보, 고객 데이터, 빌드 산출물이 포함되지 않았는지 검사한다.

## 빌드·테스트와 QA

- 코드 변경은 문법 검사나 정적 분석만으로 완료 처리하지 않는다. 변경한 구현체의 실제 빌드와 자동 테스트를 실행하고, 가능한 경우 실행 파일 또는 패키지를 구동해 핵심 흐름을 확인한다.
- Windows 전용 코드는 Windows에서, macOS 전용 코드는 macOS에서 최종 QA한다. Flutter 공통 코드는 지원 대상 플랫폼별로 빌드하고 영향을 받는 기능을 확인한다.
- 플랫폼별 QA에는 PDF 열기/끌어놓기, 페이지 이동·보기 방식·확대/축소, 검색·선택·표시, OCR, 저장/내보내기, 인쇄, 재실행 후 상태 복원, 설치/제거를 변경 범위에 맞게 포함한다.
- 인쇄는 실제 시스템 인쇄 대화상자와 PDF 저장 또는 테스트 프린터까지, 파일 권한은 실제 플랫폼 샌드박스/파일 선택 흐름까지 확인한다. 자동화로 대체할 수 없는 항목은 수동 QA 결과와 환경을 기록한다.
- 완료 전 최소한 `git diff --check`를 실행한다. 실패한 검증을 숨기거나 우회하지 않는다.

## 배포와 릴리스

- 빌드 성공은 배포 승인으로 간주하지 않는다. 사용자가 배포 대상 사용자/대상 환경, 저장소, 버전, 산출물을 명시적으로 지시한 경우에만 게시·배포한다.
- 지시가 없으면 push, 릴리스 생성, 공개 저장소 업로드, 스토어 제출, 서명·공증, 설치 파일 전달을 하지 않는다.
- 배포 전 버전 일치, 지원 OS/아키텍처, 서명·공증 상태, 체크섬, 설치 및 롤백 절차, 공개 문서와 실제 산출물 이름을 확인한다.
- 배포 결과에는 요청자, 실제 배포 수행자, 대상 사용자/환경, 릴리스 URL 또는 식별자, 체크섬과 QA 근거를 남긴다.

## UI 작업 전 필수 사항

- UI를 새로 만들거나 수정하기 전에 `docs/CY_DESIGN_SYSTEM.md`가 존재하는지 확인하고, 존재하면 먼저 읽는다. 현재 기준 커밋에는 이 문서가 없으므로 임의의 디자인 규칙을 만들어 기존 UI를 대체하지 않는다.
- 임시로 버튼을 나열하는 방식으로 기능을 추가하지 않는다.
- 화면은 문서 탐색, 선택·편집, 저장의 흐름이 한눈에 드러나야 한다.
- 새 기능에는 비어 있는 상태, 선택된 상태, 오류 상태, 저장 완료 상태의 안내를 함께 만든다.

## 디자인 원칙

- 색상, 여백, 글자 크기, 버튼 종류와 상태를 일관된 토큰으로 관리한다.
- Windows 데스크톱에서 읽기 쉽고 키보드·마우스로 쓰기 쉬운 UI를 우선한다. macOS 또는 Flutter 공통 구현을 변경할 때는 해당 플랫폼의 입력 방식과 접근성 관례도 함께 따른다.
- 외부 디자인 시스템은 구조와 원칙만 참고하고, 자산이나 코드를 무단 복제하지 않는다.
